# 2026-07-06

- 整合项目文档：将分散在 docs/ 根目录、zhihuitushu/docs/、zhihuitushu-frontend/ 下的文档统一移动到 docs/ 下，按 01-项目说明、02-需求与产品、03-技术架构、04-接口文档、05-评估与审核、06-数据库、07-第三方工具 分类，英文文件名改为中文。
- 新建 docs/00-文档目录索引.md 作为总览；更新 代码审核报告.md 中的相对链接指向新项目路径；验证全部文档为 UTF-8 编码。
- 修复管理端布局问题：App.vue 中区分移动端 H5 与管理端 Web 路由，管理端 /admin 使用全宽 admin-root 容器，不再受 mobile-container 的 480px 宽度限制；同时清理 Users.vue/Statistics.vue/Settings.vue 中重复的侧边栏与顶部栏，统一由 AdminLayout 提供布局。
- 修复图书管理页面（Books.vue）适配问题：表格外层增加横向滚动容器，设置 min-width: 1000px，避免列宽被压缩；AdminLayout.main-container 与 content-area 增加 min-width: 0，解决 flex 内容区塌陷。
- 重新设计添加/编辑图书弹窗：采用左右两栏布局（左侧基本信息/出版信息，右侧封面与状态），将封面 URL 输入替换为 el-upload 图片上传组件。
- 后端新增上传接口：UploadController 提供 POST /api/upload/cover 与 GET /api/files/covers/{filename}，本地保存至 uploads/covers，SecurityConfig 放行 /api/files/**；mvn compile 通过。
