# 2026-07-05

- 修复 zhihuitushu 后端启动失败：RedisServiceImpl 中 @Resource 注入 StringRedisTemplate 时因字段名 redisTemplate 与 Spring Boot 默认创建的 RedisTemplate<Object,Object> bean 同名导致 BeanNotOfRequiredTypeException。改为 @Resource(name = "stringRedisTemplate") 后解决。
- 将 application.properties 迁移为 application.yml，端口改为 8765，保留所有数据库/Redis/MyBatis-Plus/COS/JWT 配置。
- 通过 redis-cli 确认本地 Redis 7.4.3 在 127.0.0.1:6379 正常运行，ping 返回 PONG。
- 基于 stitch_modern_h5_mobile_ui_kit 设计系统重建 zhihuitushu-frontend：删除全部旧 Vue 页面和样式，新建 Tailwind v4 主题、App.vue、路由、底部导航与 7 个页面（Login/Register/Home/Explore/AiAssistant/Library/Profile），并移除未使用的 ElementPlus。
- 前端 build 通过，index.html 标题改为 智慧图书，vite proxy 改为 localhost:8765 以匹配新后端端口。
- 统一配色与组件风格：将 Library/Profile/Explore 中的图标色收敛到 primary/secondary/tertiary/on-surface-variant 四个语义 token，修正探索页文字为图书语境。
- 统一路由：新增路由守卫、页面 title meta、登录拦截与 guest-only 限制；路由切换加入 page 过渡动画。
- 前后端接口对接：新增 src/api/request.js（axios 拦截器 + withCredentials）与 src/api/auth.js，Login/Register 调用后端 /api/auth 接口，Profile 退出登录调用 /api/auth/logout，App.vue 启动时校验 /api/auth/me。
- H5 移动端适配：新增 mobile-container 最大宽度 480px、clamp 响应式字号与间距、safe-area 内边距、touch-target 工具类、input 16px 防 zoom。
- 移动端白+浅绿重构：基于 ui-ux-pro-max 生成清新绿设计系统，重写 main.css 颜色 token、App.vue 背景 blob、底部导航与全部 7 个页面；卡片/按钮/图标/圆角/内边距整体缩小约 15-20%，保持 44px 触控区域。
- 新配色：primary #10b981、secondary #059669、tertiary #14b8a6、background #f5fbf8，整体玻璃拟态保留但阴影与背景更柔和。
- 前端 build 通过：产物 CSS 8.59KB gzipped，JS 57.86KB gzipped。
- 修复登录/注册页布局：根容器改为 flex justify-center items-center 实现垂直+水平居中，header 改为 absolute 定位，移除 App.vue 对登录注册路由的 pb-20 叠加；压缩卡片/表单间距与头像尺寸，注册页保留 overflow-y-auto no-scrollbar 兜底。
- 前端 build 通过（managed node + 隔离 npm cache）。
- 完成登录注册功能接口对接：确认后端 AuthController/UserServiceImpl/CORS/异常处理已完善，无需改动；前端移除 request.js 中 Bearer token 拦截器，改为纯 Cookie 认证；为 Login.vue/Register.vue 增加字段级校验和错误反馈；App.vue 启动 /api/auth/me 失败时清理本地状态。
- 编写 docs/API-登录注册对接文档.md，涵盖接口规范、请求响应示例、错误码、前端对接说明、安全说明、本地联调步骤与测试用例。
- 前端 build 通过：CSS 8.64KB、JS 57.83KB gzipped。
- 接入 QQ 邮箱真实邮件验证码发送：新增 spring-boot-starter-mail 依赖、EmailService/EmailServiceImpl 服务类；VerifyCodeServiceImpl 在生成验证码写入 Redis 后调用邮件服务发送；发送失败时清除 Redis 验证码并返回错误。
- 后端 compile 成功。更新 docs/API-登录注册对接文档.md 中邮件相关说明和限制列表。
- 首页功能实现：生成 docs/首页功能PRD.md；新增 banners 表及 Mock 数据；后端新增 Book/Category/Banner 实体、Mapper、Service、Controller，开放 /api/home/banners、/api/home/categories、/api/books 接口并允许匿名访问；前端新增 api/home.js 和 api/book.js，改造 Home.vue 实现 Banner 轮播、分类入口、Tab 切换、书籍列表对接。
- 前后端 build 均通过。
- 配置 QQ 邮箱验证码发送：将用户提供的授权码填入 application.yml（username 保持 3157343935@qq.com），后端启动后用 curl 测试 /api/auth/send-code，返回 200 且邮件发送成功。
- 前端 Register.vue 已发送 type='email'，与后端 VerifyCodeServiceImpl 匹配，邮箱注册链路已跑通。
